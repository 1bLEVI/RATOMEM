function setup() {
  createCanvas(1000, 800);
   background(" white");
}

function draw() {
   fill("red");
   stroke("black")
   strokeWeight(5);
   if (mouseIsPressed){
   rect(mouseX, mouseY, 20, 30,);
 }
}
